/**
 * Dependencies.
 */

import assert from 'assert'
import SlateSchema from '..'

/**
 * Tests.
 */

describe('slate-hyperscript', () => {
  it('should import correctly', () => {
    assert.ok(SlateSchema)
  })
})
